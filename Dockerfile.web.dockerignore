.git
.github
**/node_modules
**/.next
**/__pycache__
*.log
.env
.env.*
!.env.example
# README artwork only; nothing under apps/web references it.
assets
data
deploy
docs
models
research
results
apps/api
# The runtime MiniMind directory is populated only from the verified
# conversion stage; the tracked .gitkeep placeholder and any local bundle copy
# must never enter the build context.
apps/web/public/minimind
# Tracked placeholders are never runtime assets.
**/.gitkeep
tools/requirements/*.in
tools/requirements/lock.sh
